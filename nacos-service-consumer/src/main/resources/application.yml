server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"
feign:
  hystrix:
    enabled: true
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
